# .env.test - Environment variables for the test database
# IMPORTANT: Add this file to your .gitignore!

# Option 1: Using individual variables (Recommended if DATABASE_URL is complex)
TEST_DB_HOST=localhost
TEST_DB_USER=zcanic
TEST_DB_PASSWORD=945b4886e669ffcb
TEST_DB_DATABASE=zcanic
# TEST_DB_PORT=3306 # Optional, if not default

# Option 2: Using a Database URL (Uncomment and use if preferred)
# TEST_DATABASE_URL=mysql://your_test_db_user:your_test_db_password@localhost:3306/your_test_db_name

# Ensure other necessary variables for testing are also here if they differ from .env
# For example, if you use a different JWT secret for tests:
# JWT_SECRET=your_super_secret_test_jwt_key